work(){
project=$1
workspace="/var/www"

sh ~/.dev-mat/workspace.sh;

if [ $project ]; then
	if [ -d "$workspace/$project" ]; then
		WID=$(xprop -root | grep "_NET_ACTIVE_WINDOW(WINDOW)"| awk '{print $5}')
		xdotool windowfocus $WID
		xdotool key ctrl+shift+t
		xdotool type "cd /var/www/$project; clear;"
		xdotool key Return
		wmctrl -i -a $WID
		cd /var/www/$project;
		grunt;
	else
		echo "Projeto não foi encontrado"
	fi
fi

}
eclipse(){
PASSWORD="$(cat ~/.dev-mat/.password | openssl enc -aes-256-cbc -a -d -salt -pass pass:123)"
sudo -b -p $PASSWORD -u mateus ~/eclipse/eclipse
}
